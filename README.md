
## Installation

Creating a virtual environment is highly recommended:

```bash
python3.12 -m venv .venv
source .venv/bin/activate
```

To install dependencies (and compile the C++ code in a python callable librairy)

you can run :
```bash
pip install -e .
```

